

<div class="banner mb-5" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1513470939372-bd520e7d77db?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=2089&q=80);">
  <div class="container">
    <h1>Mr TailCock - De son vrai nom Manu, saura vous faire voyager</h1>
    <p>Ce type est extrême - Cookie</p>
  </div>
</div>

<div class="container pad20">
  <div class="pad10 mb-5" style="display: flex; align-items: center; justify-content: center; ">
    <%= link_to "Add your cocktail", new_cocktail_path, class: "brk-btn"%>
  </div>
    <!-- Card -->
  <div class="cards">
    <% @cocktails.each do |cocktail| %>
      <% if cocktail.photo.attached? %>
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path cocktail.photo.key, height: 500, width: 400, crop: :fill %>')">
      <% else %>
        <div class="card-category" style="background: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3))">
      <% end %>
          <%= link_to "#{cocktail.name}", cocktail_path(cocktail), style: "color:white;"%>
          <br></br>
          <%= link_to 'x', cocktail, method: :delete, data: { confirm: 'Are you sure?' } %>
       </div>
    <% end %>
  </div>

  <!-- Add button -->


<!-- Close main div -->
</div>
